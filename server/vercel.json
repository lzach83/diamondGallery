{
  "version": 2,
  "builds": [
    {
      "src": "./dist/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./server.ts",
      "methods": ["GET", "POST", "PUT", "PATCH", "OPTIONS", "DELETE"]
    }
  ]
}
